version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install
        - yarn install
    build:
      commands:
        - yarn build
        - yarn build-storybook
  artifacts:
    baseDirectory: storybook-static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
