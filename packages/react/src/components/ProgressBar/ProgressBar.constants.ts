import { ProgressBarLabelPositions } from './ProgressBar.types';

export const defaultAnimationSpeed = 2;

export const defaultProgress = 0;

export const defaultMinProgress = 0;

export const defaultMaxProgress = 100;

export const topLabelPositions: ProgressBarLabelPositions[] = [
  'topCenter',
  'topRight',
  'topLeft',
];

export const bottomLabelPositions: ProgressBarLabelPositions[] = [
  'bottomCenter',
  'bottomRight',
  'bottomLeft',
];
