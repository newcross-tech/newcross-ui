export enum ButtonVariant {
  primary = 'primary',
  secondary = 'secondary',
}

export enum ButtonCorners {
  pill = 'pill',
  rounded = 'rounded',
}

export enum ButtonSizes {
  small = 'small',
  large = 'large',
}
