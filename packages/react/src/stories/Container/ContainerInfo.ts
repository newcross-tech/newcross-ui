const TITLE = 'Container';
const DESCRIPTION = `The 'Container' component allows us to add spacings as well as using it as a flexbox and adjusting direction and children gap. The gap is setting the childrens' margin instead of using the css 'gap' for cross compatibility.`;

const DO = ['Use this component as a wrapper to your custom components'];

const DONT = [
  `Unneccessarily create a new component that can use 'Container' as it's base.`,
];

export { TITLE, DESCRIPTION, DO, DONT };
