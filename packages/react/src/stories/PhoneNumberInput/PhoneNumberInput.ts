const TITLE = 'Phone Input';
const DESCRIPTION =
  'Phone fields allow users to input phone number, usually when required to fill out a form';

const DO = [
  'Use in conjunction with other text fields, stacked vertically.',
  'Use with labels only',
  'Type numbers only',
];

const DONT = ['Type letters'];

export { TITLE, DESCRIPTION, DO, DONT };
