version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
        - yarn build-storybook
  artifacts:
    baseDirectory: storybook-static
    files:
      - 'storybook-static/*'
  cache:
    paths:
      - node_modules/**/*
